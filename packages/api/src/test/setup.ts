// Preloaded before every test run. Stub heavy services here once they exist.
// e.g. after an emails opt-in:
//   const { txMailer } = await import('@core/mailer');
//   txMailer.send = async () => ({ ok: true });
export {};
